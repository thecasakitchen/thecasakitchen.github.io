import React, { useEffect } from "react";
import { Container, NavSubBar } from "../Styles";

const Home = (props) => {

    return (
        <React.Fragment>
            <NavSubBar>Home</NavSubBar>
            <Container>
                Home
            </Container>
        </React.Fragment>
    );
};

export default Home;