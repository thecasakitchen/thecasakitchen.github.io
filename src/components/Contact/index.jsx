import React from "react";

import { NavSubBar, FlexRow, FlexColumn } from "../Styles";

const Contact = (props) => {
    
    return (
        <React.Fragment>
            <NavSubBar>Contact</NavSubBar>
            <FlexRow>

            </FlexRow>
            <FlexRow>
                <FlexColumn></FlexColumn>
                <FlexColumn></FlexColumn>
            </FlexRow>
        </React.Fragment>
    );
};

export default Contact;